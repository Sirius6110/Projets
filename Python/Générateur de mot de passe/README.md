# 🔑 Générateur de Mots de Passe Sécurisé (Python)

## 📝 Description du Projet

Ce projet est un outil simple mais robuste développé en Python. Il génère des mots de passe **aléatoires** et **sécurisés** en se basant sur les choix de l'utilisateur (longueur, inclusion de chiffres et de caractères spéciaux).

L'objectif principal de ce projet était de pratiquer les concepts de base de Python :
* **Gestion des entrées** utilisateur (`input()` et validation des types).
* **Structures de contrôle** (`while`, `if/else`).
* **Boucles** (`for`) pour la génération itérative.
* **Manipulation de chaînes** de caractères et de listes.
* Implémentation du module standard **`random`** pour le tirage aléatoire des caractères.
* Application d'une logique de **sécurité** pour garantir la présence d'au moins un caractère de chaque catégorie sélectionnée.

## 🛠️ Technologies Utilisées

* **Langage :** Python 3.x
* **Modules Standards :** `random`, `string`
    
## 🚀 Installation et Utilisation

### Prérequis

Assurez-vous que Python 3 est installé sur votre système.

### Lancement du programme

1.  **Enregistrez** le code du générateur de mots de passe dans un fichier nommé, par exemple, `generateur.py`.
2.  **Ouvrez** votre terminal ou invite de commande.
3.  **Lancez** le script avec la commande suivante :

```bash
python generateur.py